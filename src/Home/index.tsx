import React from 'react';
import { Text, SafeAreaView } from 'react-native';

const Home: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Home Screen</Text>
    </SafeAreaView>
  );
};

export default Home;
